<h2 align="center">補充習題</h2>

```
CH1

有關 Javascript 之敘述，說法完全正確的是：

A. 陣列內所儲存的資料，必須為同一種型別
B. 物件是一種存放資料的集合，具有索引的屬性
C. Javascript 屬於事件驅動，執行前需要通過編譯
D. 在未經關鍵字宣告下直接使用變數，執行時也不會報錯
```
```
CH2

以下哪些變數的宣告是正確的：

A. const myMoney;
B. let a - 1 = 6;
C. var $exam_001 = null;
D. let 2_do = [1, 2, 3];
```
```
CH2

以下何者運算結果為 true?

A. "25" > "100"
B. "7" - 0 === 7 + "0"
C. "8" * "12" === "96"
D. "4" + 13 % "3" == 5
```
```
CH3

(function fn(){
    var fn = "Monday";
    console.log(fn());
    function fn(){
        return 2024;
    }
})();
```
```
CH4

if(!(a in window)){
    var a = 4;
}
console.log(a);
```
```
CH5

function Foo(){
    this.text = "text1";
    function bar(){
        this.text = "text2";
    }
    bar();
}
Foo.text = "test3";

var foo = new Foo();
console.log(foo.text);
```
```
CH5

var obj = {
    fn: function(){
        return this.value;
    },
    value: 22
};

(function(f){
    console.log(arguments[0]());
})(obj.fn);
```
```
CH7

var a = "Hello";
var b = [43.3, a];
var c = b;

b = b.join("+").split("").reverse();
console.log(b.length - c.length);
```
```
CH8

關於原型鍊及繼承的敘述，何者說法有誤?

A. 使用 new 運算子時，this 會指向於創建的實體上
B. 實體會具有 prototype 的屬性，指向它所繼承的原型
C. prototype 具有 constructor 屬性，指向它自身的原型
D. 多層的繼承會產生原型鍊，最終指向物件 Object.prototype
```
```
CH8

var employee = {
    company: "XYZ"
}
var emp1 = Object.create(employee);
emp1.company = "ABC";
delete emp1.company;
console.log(emp1.company);
```
```
CH9

下列程式碼的執行優先順序為：

A. setTimeout(function(){
       console.log("YYY");
   }, 0)
B. Promise.resolve()
       .then(function(){
           console.log("ZZZ");
       })
C. console.log("XXX");
```
```
CH10

以下正規表達式的比對結果，配對正確的是：

var str = /\d+[abc]/;

A. str.test("abc123"): []
B. "123abc".search(str): 0
C. "abc123".match(str): true
D. "123abc".indexOf(str): null
```
<br>

參考答案與解析：
